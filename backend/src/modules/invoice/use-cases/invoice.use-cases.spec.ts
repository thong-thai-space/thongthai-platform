import { InvoiceUseCases } from './invoice.use-cases';

describe('InvoiceUseCases', () => {
  it.todo('add use-case tests');

  it('should construct', () => {
    expect(InvoiceUseCases).toBeDefined();
  });
});
