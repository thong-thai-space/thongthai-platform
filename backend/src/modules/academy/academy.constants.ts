// DI symbol + tunables for the Academy module (Dịch vụ 2 — Training & Playbook).

export const ACADEMY_REPOSITORY = Symbol('ACADEMY_REPOSITORY');

export const DEFAULT_PAGE_SIZE = 25;
export const MAX_PAGE_SIZE = 100;
